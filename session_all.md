# 커플 채팅 앱 - 대화 시간 연장 기능 구현 가이드

## 기능 개요
AI 상담사와 함께하는 커플 채팅 앱에서 상황 대화 시간이 종료될 때의 연장 기능 구현

## 시스템 플로우

### 전체 세션 구조
```
대화 단계별 연장 제한:
- 상황 대화: 최대 1번 연장 가능 (초기 + 연장1)
- 감정 대화: 최대 1번 연장 가능 (초기 + 연장1)
- 모든 단계 완료 후: 채팅 종료
```

### 1. 상황 대화 시간 종료 - 첫 번째 연장 기회
```
조건: 상황 대화 연장 횟수 < 1회
상황: 상황에 대한 대화 시간이 종료됨
동작: 
- 두 사용자 모두에게 동시에 질문 표시
- 질문: "상황에 대한 대화 시간이 종료되었습니다. 계속하시겠습니까?"
- 선택지: 
  * "네. 아직 시간이 필요합니다"
  * "아니오. 넘어가겠습니다"
```

### 1-1. 상황 대화 시간 종료 - 최대 연장 횟수 도달
```
조건: 상황 대화 연장 횟수 = 1회 (최대치 도달)
상황: 상황에 대한 대화 시간이 종료됨
동작:
- 질문 없이 자동으로 다음 단계 진행
- 두 사용자 모두에게 메시지 전송
- 메시지: "이제 감정에 대한 대화를 시작하겠습니다"
- 새로운 감정 대화 session을 DB에 추가
```

### 2. 사용자 선택 대기 상태
```
조건: 한 명의 사용자가 "네" 선택, 다른 사용자 미선택
동작:
- 선택한 사용자에게만 메시지 전송
- 메시지: "다른 사용자가 선택 중입니다..잠시만 기다려주세요"
- 다른 사용자의 선택을 계속 대기
```

### 3. 양쪽 모두 "네" 선택
```
조건: 사용자A = "네", 사용자B = "네"
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "상황에 대한 대화를 15분 연장하겠습니다"
2. 데이터베이스 작업
   - 새로운 session 생성 및 추가
   - topic: "topic_1_situation"
   - duration: 15분
3. 상황 대화 모드 계속 진행
```

### 4. 한쪽 "네", 다른 쪽 "아니오" 선택
```
조건: 사용자A = "네", 사용자B = "아니오" (또는 그 반대)
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "상황에 대한 대화를 15분 연장하겠습니다"
2. 데이터베이스 작업
   - 새로운 session 생성 및 추가
   - topic: "topic_1_situation"
   - duration: 15분
3. 상황 대화 모드 계속 진행
```

### 5. 양쪽 모두 "아니오" 선택 (상황 대화)
```
조건: 사용자A = "아니오", 사용자B = "아니오"
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "이제 감정에 대한 대화를 시작하겠습니다"
2. 데이터베이스 작업
   - 새로운 session 생성 및 추가
   - topic: "topic_2_emotion"
   - extension_count: 0 (감정 대화 연장 횟수 초기화)
   - 감정 대화 관련 설정 적용
3. 감정 대화 모드로 전환
```

### 6. 감정 대화 시간 종료 - 첫 번째 연장 기회
```
조건: 감정 대화 연장 횟수 < 1회
상황: 감정에 대한 대화 시간이 종료됨
동작: 
- 두 사용자 모두에게 동시에 질문 표시
- 질문: "감정에 대한 대화 시간이 종료되었습니다. 계속하시겠습니까?"
- 선택지: 
  * "네. 아직 시간이 필요합니다"
  * "아니오. 넘어가겠습니다"
```

### 6-1. 사용자 선택 대기 상태 (감정 대화)
```
조건: 한 명의 사용자가 "네" 선택, 다른 사용자 미선택
동작:
- 선택한 사용자에게만 메시지 전송
- 메시지: "다른 사용자가 선택 중입니다..잠시만 기다려주세요"
- 다른 사용자의 선택을 계속 대기
```

### 6-2. 양쪽 모두 "네" 선택 (감정 대화)
```
조건: 사용자A = "네", 사용자B = "네"
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "감정에 대한 대화를 15분 연장하겠습니다"
2. 데이터베이스 작업
   - 새로운 session 생성 및 추가
   - topic: "topic_2_emotion"
   - extension_count 증가
   - duration: 15분
3. 감정 대화 모드 계속 진행
```

### 6-3. 한쪽 "네", 다른 쪽 "아니오" 선택 (감정 대화)
```
조건: 사용자A = "네", 사용자B = "아니오" (또는 그 반대)
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "감정에 대한 대화를 15분 연장하겠습니다"
2. 데이터베이스 작업
   - 새로운 session 생성 및 추가
   - topic: "topic_2_emotion"
   - extension_count 증가
   - duration: 15분
3. 감정 대화 모드 계속 진행
```

### 6-4. 양쪽 모두 "아니오" 선택 (감정 대화)
```
조건: 사용자A = "아니오", 사용자B = "아니오"
동작:
1. 두 사용자 모두에게 메시지 전송
   메시지: "채팅을 종료합니다"
2. 데이터베이스 작업
   - 현재 session 의 end_time 을 현재 시간으로 설정
```

### 7. 감정 대화 시간 종료 - 최대 연장 횟수 도달
```
조건: 감정 대화 연장 횟수 = 1회 (최대치 도달)
상황: 감정에 대한 대화 시간이 종료됨
동작:
- 질문 없이 자동으로 채팅 종료
- 두 사용자 모두에게 메시지 전송
- 메시지: "채팅을 종료합니다"
- 현재 session 의 end_time 을 현재 시간으로 설정
```

## 구현 시 고려사항

### 기술적 요구사항
- **실시간 통신**: WebSocket 또는 Server-Sent Events를 통한 실시간 상태 동기화
- **상태 관리**: 각 사용자의 선택 상태를 서버에서 추적
- **세션 관리**: 기존 세션 종료 및 새 세션 생성 로직
- **UI/UX**: 버튼 비활성화, 로딩 상태 표시

### 데이터베이스 스키마 예시
```sql
sessions 테이블:
- sessio n_id (Primary Key)
- couple_id (Foreign Key)
- session_type (situation/situation_extended/emotion/emotion_extended)
- phase (situation/emotion) -- 현재 대화 단계
- extension_count (0, 1) -- 각 단계별 연장 횟수
- start_time
- duration
- status (active/completed)
- created_at

couples 테이블:
- couple_id (Primary Key)
- user1_id
- user2_id
- current_session_id
- situation_extensions (0, 1) -- 상황 대화 연장 횟수
- emotion_extensions (0, 1) -- 감정 대화 연장 횟수
```

### 전체 플로우 다이어그램
```
시작
│
├─ 상황 대화 (15분)
│   ├─ 시간 종료 → 연장 질문 (1차)
│   │   ├─ 모두 네 → 상황 대화 연장 (15분)
│   │   │   └─ 시간 종료 → 자동으로 감정 대화 시작
│   │   ├─ 한쪽 네, 한쪽 아니오 → 상황 대화 연장 (15분)
│   │   │   └─ 시간 종료 → 자동으로 감정 대화 시작
│   │   └─ 모두 아니오 → 감정 대화 시작
│   └─ (1차 연장 후)
│
├─ 감정 대화 (15분)
│   ├─ 시간 종료 → 연장 질문 (1차)
│   │   ├─ 모두 네 → 감정 대화 연장 (15분)
│   │   │   └─ 시간 종료 → 자동으로 채팅 종료
│   │   ├─ 한쪽 네, 한쪽 아니오 → 감정 대화 연장 (15분)
│   │   │   └─ 시간 종료 → 자동으로 채팅 종료
│   │   └─ 모두 아니오 → 채팅 종료
│   └─ (1차 연장 후)
│
└─ 채팅 종료
```

### 에러 처리
- 네트워크 연결 끊김 시 재연결 로직
- 선택 타임아웃 처리 (예: 5분 후 자동 "아니오" 선택)
- 동시성 처리 (Race Condition 방지)

### 사용자 경험 고려사항
- 선택 후 버튼 비활성화로 중복 선택 방지
- 대기 중 진행 상황 표시
- 명확한 안내 메시지 제공
- 현재 대화 단계 및 연장 가능 횟수 표시
- 자동 전환 시 명확한 안내 메시지 제공

### 구현 체크리스트
- [ ] 연장 횟수 추적 시스템 구현
- [ ] 단계별 최대 연장 제한 로직
- [ ] 자동 전환 메커니즘 (연장 한도 도달 시)
- [ ] 각 단계별 메시지 템플릿 준비
- [ ] 세션 상태 관리 및 DB 스키마 업데이트
- [ ] 실시간 상태 동기화 강화
- [ ] 예외 상황 처리 로직 완비